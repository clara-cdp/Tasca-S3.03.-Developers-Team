<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo list</title>
</head>

<body>
    <h1>Ta-Da! </h1>

    <div id="create">
        <a href="<?= WEB_ROOT ?>/create"> <button id="createTask"> + </button></a>
    </div>

    <form action="home" method="POST">
        <input type="text" name="keyWord">
        <input type="submit" value="search">🔍 <!-- TODO: press search twice -> error / goes to view all -->
    </form>

    <a href="home" style="text-decoration: none;">View all tasks</a>

    <div class="taskContainer">


        <?php $displayTasks = $this->view->tasks ?? $this->tasks ?? [];
        $isSearch = $this->view->isSearch ?? $this->isSearch ?? false; ?>

        <?php if (empty($displayTasks)): ?>

            <?php if ($isSearch): ?>

                <p>⚠️ No tasks found... Try again!</p>
            <?php else: ?>
                <p>✅ All Done! Use the "+" button to add a new task.</p>
            <?php endif; ?>

        <?php else: ?>
            <?php foreach ($displayTasks as $task): ?>
                <div class='taskBox' style="border: 1px solid black; width:200px; height:wrap; padding:3px;" ;>
                    <h3><?php echo htmlspecialchars($task['name']); ?></h3>
                    <p><?php echo htmlspecialchars($task['description']); ?></p>
                    <p><?php echo htmlspecialchars($task['created_at']); ?></p>
                    <?php /*
                    <p>Status: <?php echo $task['status']; // need to add status to 'task'
                                ?></p>*/ ?>
                    <a href="update?id=<?php echo $task['id']; ?>"><button>✏️</button></a>
                    <!-- TODO: find edit logo -->
                    <a href="delete?id=<?php echo $task['id']; ?>" onclick="return confirm('Are you sure you want to delete this task?');">
                        <button>🗑️</button></a>
                    <!-- TODO: find bin logo-->

                </div>
            <?php endforeach; ?>
        <?php endif; ?>

    </div>









</body>

</html>